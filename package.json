{
  "name": "log",
  "private": true,
  "scripts": {
    "build": "esbuild action.ts --bundle --format=cjs --platform=node --legal-comments=none --outfile=action.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.0",
    "esbuild": "^0.18.0",
    "typescript": "^5.1.3"
  }
}
