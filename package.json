{
  "name": "log",
  "private": true,
  "scripts": {
    "build": "esbuild action.ts --bundle --minify --format=cjs --platform=node --legal-comments=none --outfile=action.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.2.3",
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4"
  }
}
